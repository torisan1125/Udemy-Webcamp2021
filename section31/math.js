const add  = (x, y) => x+ y;

const PI = 3.14579

const square = (x ,x) => x * x;

module.exports.add = add;
module.exports.PI = PI;
module.exports.square = square;


const math = {
    add: add,
    PI: PI,
    square: square
}